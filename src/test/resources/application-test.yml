spring:
  datasource:
    url: jdbc:mysql://localhost:3306/siljeun_test
    username: root
    password: q1w2e3r4
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none  # 이미 생성된 테이블 기준 테스트
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2s
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          min-idle: 1
  cache:
    type: redis
